'use client';

import { Box, Typography } from '@mui/material';

export default function CostsPage() {
    return (
        <Box p={2}>
            <Typography variant="h4" gutterBottom>
                Costs
            </Typography>
            <Typography>Conteúdo da página de custos...</Typography>
        </Box>
    );
}
